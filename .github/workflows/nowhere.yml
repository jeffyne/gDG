name: importance

on:
  push:

jobs:
  retain:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        port: ["agency", "picky", "inexpensive", "copper", "maximum"]
        clothing: ["dam", "compensate", "drum", "magnificent"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar gB.jar
